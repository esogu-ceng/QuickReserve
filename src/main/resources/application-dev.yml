spring:
  application:
    name: QuickReserve
  datasource:
    url: jdbc:postgresql://localhost:5432/QuickReserve
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect